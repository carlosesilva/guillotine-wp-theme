import React from "react";
import ReactDOM from "react-dom";

const domContainer = document.querySelector("#guillotine-options-root");
ReactDOM.render(<div>All the cool options will go here</div>, domContainer);
