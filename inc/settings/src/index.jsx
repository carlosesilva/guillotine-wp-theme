import React from "react";
import ReactDOM from "react-dom";
import Settings from "./components/Settings";

const domContainer = document.querySelector("#guillotine-settings-root");
ReactDOM.render(<Settings />, domContainer);
